version: '3.8'

services:
  server:
    build:
      context: ./backend
      dockerfile: Dockerfile
    volumes:
      - ./backend/:/backend/
    command: uvicorn server:app --reload --host 0.0.0.0 --port 8000
    env_file:
      - ./backend/src/.env
    ports:
      - 8000:8000

